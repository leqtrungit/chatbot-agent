# Progress

Living document tracking project status. Update this file in the same commit as the work it describes (add a row to the log, move backlog items when picked up).

## Status

| Area | State | Notes |
|---|---|---|
| Infra (docker compose) | ✅ Done | postgres+pgvector :5432, pgadmin :5050, redis :6379, arq worker container |
| Backend core (config/db/auth) | ✅ Done | basic auth admin/admin via env |
| Domain CRUD | ✅ Done | `/api/domains`, uuid + slug |
| Document ingestion | ✅ Done | PDF/DOCX/TXT/MD → extract → chunk → embed → pgvector (HNSW cosine), async via arq |
| Agent framework (`app/agent/`) | ✅ Done | pure library: builder, dynamic tool loop, prompt templates, skills, Ollama + OpenAI-compatible adapters |
| Webhook + jobs (async AI branch) | ✅ Done | `POST /api/webhooks/{platform}` → queue → worker → `GET /api/jobs/{id}` polling |
| SSE chat streaming | ✅ Done | `POST /api/chat/stream` — same arq queue/rate-limiter, worker streams tokens to Redis pub/sub, API relays as SSE; playground streams instead of polling |
| Webhook auth + rate limiting | ✅ Done | `app/modules/apikey/` (admin CRUD, `X-API-Key`), fixed-window Redis rate limit per key + per session (Phase A of webhook-auth/memory/OpenAI-provider plan) |
| Conversation memory | ✅ Done | `app/modules/conversation/` (`chat_messages` table, migration `e717eccd3cad`), worker loads last `CHAT_HISTORY_LIMIT` turns and persists each turn after the agent replies (Phase B of same plan) |
| Channels | ✅ Generic only | `GenericAdapter`; real platforms not integrated yet |
| User-managed agents | ✅ Done | `app/modules/agent/` (`Agent` CRUD, provider/model/tools per row, free-text `system_prompt`) + `app/modules/mcp/` (registered HTTP/SSE MCP servers, M2M to agents); `domain_agents`/`agent_mcp_servers` M2M tables. `build_agent` (worker) is agent-row-driven; a chat request only ever names `agent_id` — the agent searches across all of its own assigned domains itself via a multi-domain `knowledge_search` tool, no `domain_id` in the request contract at all |
| Frontend admin | ✅ Done | login, domains CRUD, documents upload + status, chat playground (Next.js 16, agent-only picker), API keys management, Agents + MCP Servers management (drawer-based create/edit), domain↔agent assignment |
| Tests | ✅ 212 passing | TDD, LLM/embedding fully mocked |
| Real-LLM e2e | ⏳ Blocked | Ollama not installed on host yet |
| Git workflow | ✅ Done | `develop` (direct push, no force-push/deletion) → `main` (PR-only, enforced for admins, no required approvals) via GitHub branch protection; see README "Git workflow" |
| CI | ❌ Not started | no automated checks gate the `main` PR yet — branch protection enforces "PR required", not "tests pass" |
| Deployment | ❌ Not started | local docker compose only |

## Backlog (rough priority)

1. Install Ollama on host (`ollama pull qwen2.5 nomic-embed-text`) and run a real chat e2e through the playground (now streaming via SSE).
2. First real channel adapter (Telegram or Slack) — follow `.claude/skills/add-channel-adapter/`.
3. CI: GitHub Actions running `uv run pytest -q` (postgres+redis services) and `npm run build`.
4. Push-based responses: implement `send_response()` for platforms that support it (today: polling only).
5. Replace hardcoded basic auth with real user accounts when multi-admin is needed.
6. Ingestion niceties: re-ingest/retry failed documents from the UI, chunk size tuning per domain.
7. ~~Conversation memory~~ — done (see Phase B log entry below).
8. ~~Phase C of webhook-auth/memory/OpenAI-provider plan~~ — done (see log below).
9. ~~User-managed agents (provider/model/tools/MCP + domain assignment)~~ — done (see log below).
10. MCP stdio/subprocess transport support (today: HTTP/SSE only, by design).
11. Encrypt `agents.api_key` / `mcp_servers.headers` at rest instead of plaintext, if this ever leaves a trusted deployment.

## Log

| Date | Change |
|---|---|
| 2026-07-08 | Kickoff: full platform built (BE 100 tests, FE build green, worker container running); repo pushed to `leqtrungit/chatbot-agent` |
| 2026-07-08 | Docs: README, CLAUDE.md, 4 project skills (`verify`, `add-channel-adapter`, `extend-agent`, `db-migration`) |
| 2026-07-08 | Added this progress tracker |
| 2026-07-11 | Phase A of webhook-auth/memory/OpenAI-provider plan: `app/modules/apikey/` (model+migration `e272b45380b3`, admin CRUD), `require_api_key` on webhook + job-status endpoints, `app/core/ratelimit.py` fixed-window Redis limiter (per-key + per-session, 429 + Retry-After), FE `api-keys` page + nav + playground API key input. BE 117 tests passing, FE build green. Commit `96a72e7`. |
| 2026-07-11 | Phase B of same plan: `app/modules/conversation/` (`ChatMessage` model + migration `e717eccd3cad`, `load_history`/`append_turn`), new setting `CHAT_HISTORY_LIMIT` (default 20), `process_chat_job` now loads history before `agent.run(text, history=...)` and persists the user+assistant turn after a successful reply (nothing persisted if the agent raises). `app/agent/` untouched. BE 122 tests passing. Commit `9d8bc1b`. |
| 2026-07-11 | Phase C of same plan: `app/agent/providers/openai_compat.py` (`OpenAICompatProvider`, OpenAI Chat Completions mapping — Bearer auth, tool_calls with JSON-string arguments and real echoed ids, tool-result messages, `ModelParams`→temperature/top_p/max_tokens/stop/seed+extra, `prompt_tokens`/`completion_tokens` usage), exported from `app/agent/providers/__init__.py`. New settings `LLM_PROVIDER` (`ollama`\|`openai`), `OPENAI_BASE_URL`, `OPENAI_API_KEY`; `build_llm_provider` branches on `LLM_PROVIDER` and raises `ValueError` on an unknown value. `.env.example`/`.env` updated (default stays `ollama`). `app/agent/core/agent.py` and `builder.py` untouched. BE 135 tests passing. Commit `8d029c8`. |
| 2026-07-22 | SSE chat streaming, built as 6 independent tasks against a frozen contract (agent-core streaming types unblocked the rest, then providers/worker/endpoint/frontend ran in parallel): `StreamChunk`/`AgentStreamEvent` + `LLMProvider.chat_stream` + `Agent.run_stream` (`run()` untouched) in `app/agent/`; `chat_stream` implemented for both `OllamaProvider` (NDJSON) and `OpenAICompatProvider` (SSE, tool-call deltas accumulated by index); new arq task `process_chat_job_stream` publishes `token`/`done`/`error` messages to Redis channel `chat:job:{job_id}` (existing `process_chat_job` untouched); new `app/modules/chat/` module exposes `POST /api/chat/stream` reusing the webhook module's auth/rate-limit/domain-resolution, subscribing to the pub/sub channel *before* enqueueing to avoid a publish-before-subscribe race; frontend playground now streams tokens via `fetch`+manual SSE parsing instead of polling (old poll path kept as fallback). BE 176 tests passing (+41). Live SSE smoke test verified queued→error frame plumbing end-to-end (no Ollama on this host, same known gap as before) and confirmed `GET /api/jobs/{id}` still resolves as a fallback. Commits `215c102`..`7a92e24`. |
| 2026-08-06 | User-managed agents: replaced the single global-`Settings`-driven agent with admin-configurable `Agent` rows (provider, model, credentials, system-prompt-template, max_iterations, temperature/top_p, `enable_knowledge_search`) and `McpServer` rows (HTTP/SSE only, admin-registered). Many-to-many `domain_agents` + `agent_mcp_servers` join tables (migration `253d492b3b04`); new `app/modules/agent/` and `app/modules/mcp/` (CRUD + `PUT /api/agents/{id}/domains` / `PUT /api/domains/{id}/agents`). New `app/agent/tools/mcp.py` (`connect_mcp_server`/`McpTool`/`list_mcp_tools`, pure — only depends on the `mcp` SDK, no DB) plus `app/modules/mcp/client.py` gluing DB rows to live tool connections. `build_domain_agent` now takes `(domain, agent)` instead of `(domain, settings)`; `process_chat_job`/`process_chat_job_stream` load the `Agent` row and open an `AsyncExitStack` for MCP connections around the run. Public API is now explicit rather than implicit: `IncomingMessage`/`ChatStreamRequest` require `agent_id`, and the webhook/chat-stream routers 403 if the named agent isn't linked to the resolved domain (`webhook/service.resolve_agent_for_domain`) — this is a breaking change to the webhook/chat-stream payload contract, no backward-compat shim by design (per-conversation decision, no existing domains to migrate). Frontend: new `/agents` and `/mcp-servers` admin pages, domain detail page gained an assigned-agents checklist, playground drawer gained an agent selector scoped to the chosen domain. BE 210 tests passing (+34), FE build/lint green. Live smoke test against a real Postgres + worker container confirmed the full path (webhook → agent/domain validation → job → worker resolves the agent-specific `OllamaProvider` and calls the real endpoint, failing only on a not-pulled model name — expected on this host). |
| 2026-08-06 | Agent UX overhaul, run as 3 parallel subagents against a frozen contract: (1) backend — `Agent.system_prompt_template` (jinja template-name picker, `PromptLoader`-validated) replaced with a free-text optional `Agent.system_prompt: str \| None` column (migration `e28f863c84bc`), used verbatim in `build_domain_agent` with no domain name/description auto-injection (agent already has `KnowledgeSearchTool` to look up domain content on demand); empty falls back to `AgentBuilder`'s own `DEFAULT_SYSTEM_PROMPT` (`app/agent/core/builder.py` untouched, per hard rule); (2) `frontend/src/app/(admin)/mcp-servers/page.tsx` create/edit forms converted `Dialog`→`Sheet` (drawer, same pattern as `PlaygroundDrawer`) — user feedback that modals felt cramped; (3) `frontend/src/app/(admin)/agents/page.tsx` same Dialog→Sheet conversion, plus the system-prompt-template `Input` replaced with a free-text `Textarea`, plus the domain-assignment checklist moved from a buried spot near the bottom to a prominent bordered "Serves these domains" section right after the Name field. BE 213 tests passing (+3), FE build/lint green. Live browser smoke test (create-domain → open drawer → check domain → write custom system prompt → submit → verify via API) confirmed `system_prompt` round-trips verbatim and `domain_ids` set correctly end-to-end. |
| 2026-08-06 | Agent-scoped chat redesign, run as 4 parallel subagents against a frozen contract: a chat request now only ever names `agent_id` — `domain_id` is gone from the webhook and `/api/chat/stream` payloads entirely, and so is the "agent not assigned to this domain" 403 check (nothing left to check — the agent's own `agent.domains` M2M *is* its scope now). (1) `app/agent/tools/knowledge_search.py::KnowledgeSearchTool` takes `domains: list[dict]` instead of a single `domain_id`; single-domain agents get the exact same schema/behavior as before (fully backward compatible), 2+-domain agents get a `domain` enum parameter (domain slugs) the LLM must pick per call, letting one agent search across all the knowledge bases it's assigned to within a conversation. (2) `chat_messages` re-keyed from `(domain_id, session_id)` to `(agent_id, session_id)` (migration `545353cf397f`, table truncated — no domain→agent data-preserving path exists, dev data only) since a conversation now belongs to an agent, not a single domain. (3) `app/worker/tasks.py::build_domain_agent` renamed `build_agent`, drops its `domain` param, builds the `knowledge_search` tool from `agent.domains` (skipped entirely if the agent has zero domains assigned); `process_chat_job`/`process_chat_job_stream` drop `domain_id`; `app/modules/webhook/service.py` deleted outright (its domain-resolution helpers became dead code); `IncomingMessage`/`ChatStreamRequest`/`GenericAdapter` all drop `domain_id`. (4) Playground drawer picks an Agent directly (no more Domain→Agent cascading selects). `app/modules/agent/`, `app/modules/domain/`, `app/agent/core/{agent,builder}.py` untouched. BE 212 tests passing, FE build/lint green. Live verification: real webhook call with only `{agent_id, message}` (no `domain_id`) → 202; a real 2-domain agent's `build_agent()` produced a `knowledge_search` tool with `domain` enum `["hr-policies", "sales-playbook"]`, and a scripted LLM tool call naming `domain: "sales-playbook"` resolved to exactly that domain's real UUID when calling the searcher — full slug→domain_id resolution confirmed against live Postgres. |
| 2026-08-07 | Domain-picker UX fix (checkbox list → wrapped chip toggle in the agent form, scales to ~10 domains/org without scrolling) + resilience fix (agent/domain-detail pages used `Promise.all` for parallel fetches, so one failing request silently blanked out unrelated data that had already loaded — switched to `Promise.allSettled`). Landed everything from the last few sessions via PR #2 (`feature/user-managed-agents` → `main`, squash-merged, branch deleted). Set up a `develop`/`main` git workflow: created `develop` from `main`; branch protection on `main` (PR required, enforced for admins, no force-push/deletion, 0 required approvals — solo repo) and on `develop` (no force-push/deletion, direct push allowed). Docs updated to match current architecture: `CLAUDE.md` (Architecture section, hard rules), `README.md` (features, API table, new "Git workflow" section), `.claude/skills/extend-agent/SKILL.md` and `.claude/skills/verify/SKILL.md` (both referenced the old `build_domain_agent`/template-picker/domain_id-in-webhook shape). |

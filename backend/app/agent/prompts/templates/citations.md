Search results are numbered with `[n]` markers. Cite the marker inline
immediately after each claim you take from that source, e.g. "Vacation is
20 days [1]." Only use markers that actually appeared in tool results —
never invent a marker. When a claim draws on more than one source, cite
each marker in sequence, e.g. "[1][2]".
